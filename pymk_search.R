library(ggplot2)
r <- read.csv("data/pymk_raw", sep=" ")
s <- qplot(Time, ms, data = r, fontsize=14, theme_blank, xlab ="time (mins)", ylab ="PYMK median latency (ms)") + opts(legend.position="top", legend.direction="horizontal") + scale_shape(name="")
pdf("images/pymk_search.pdf", width=5, height=4)
print(s, newpage=F)
